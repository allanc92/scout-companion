name: Deploy main to Azure (MANUAL ONLY)

# Manual deployment workflow - automatic deployment handled by Azure
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual deployment'
        required: false
        default: 'Manual deployment trigger'

jobs:
  manual-deploy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Manual Deployment Notice
        run: |
          echo "🔧 Manual deployment workflow triggered"
          echo "Reason: ${{ github.event.inputs.reason || 'Manual deployment trigger' }}"
          echo "⚡ Azure handles automatic deployment from main branch"
          echo "✅ No action needed - this is just a notification workflow"
